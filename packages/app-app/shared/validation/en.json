{
  "amount": {
    "min": "Amount must be greater than 0",
    "required": "Amount is required"
  },
  "billingCycle": {
    "invalid": "Invalid billing cycle"
  },
  "botDisableReason": {
    "required": "Bot stop reason is required"
  },
  "bridgeChainId": {
    "invalid": "Invalid chain ID",
    "required": "Destination chain is required"
  },
  "bridgeRecipientAddr": {
    "invalid": "Invalid recipient address",
    "required": "Recipient address is required"
  },
  "bridgeTokenAddress": {
    "required": "Token address is required"
  },
  "buySizeRange": {
    "minGreaterThanMax": "Minimum buy shares must be less than or equal to maximum buy shares"
  },
  "datePreset": {
    "invalid": "Invalid date preset"
  },
  "marketInterval": {
    "invalid": "Invalid interval value"
  },
  "marketSymbol": {
    "invalid": "Invalid market symbol"
  },
  "privateKey": {
    "invalid": "Invalid private key format (must be 0x + 64 hex characters)",
    "required": "Private key is required"
  },
  "publicKey": {
    "invalid": "Invalid RSA public key format",
    "required": "Public key is required"
  },
  "solanaAddress": {
    "invalid": "Invalid Solana address format (must be 32-44 base58 characters)"
  },
  "strategyId": {
    "required": "Strategy ID is required"
  },
  "strategyJson": {
    "required": "Strategy configuration is required"
  },
  "strategyName": {
    "required": "Strategy name is required",
    "tooLong": "Strategy name must be {maximum} characters or less"
  },
  "subscriptionPlan": {
    "invalid": "Invalid subscription plan"
  },
  "timeRange": {
    "customEndRequired": "End time is required for custom date range",
    "customStartRequired": "Start time is required for custom date range",
    "exceedsMaxSpan": "Date range cannot exceed {days} days",
    "startAfterEnd": "Start time must be less than or equal to end time"
  },
  "tronAddress": {
    "invalid": "Invalid Tron address format (must start with T, 34 base58 characters)"
  },
  "walletAddress": {
    "invalid": "Invalid wallet address format (must be 0x + 40 hex characters)",
    "required": "Wallet address is required"
  },
  "walletName": {
    "required": "Wallet name is required",
    "tooLong": "Wallet name must be {maximum} characters or less"
  }
}